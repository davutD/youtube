const userDatabase = require('./user-database')
const videoDatabase = require('./video-database')
const commentDatabase = require('./comment-database')

module.exports = {
  userDatabase,
  videoDatabase,
  commentDatabase,
}
